{
  "name": "asafarim-digital",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "dev:portal": "turbo dev --filter=portal",
    "dev:ops": "turbo dev --filter=ops-hub",
    "onVPS:dockerUp": "docker compose up -d --force-recreate portal content-generator ops-hub",
    "onVPS:dockerDown": "docker compose down"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AliSafari-IT/asafarim-digital.git"
  }
}